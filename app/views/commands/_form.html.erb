<%= simple_form_for @command, html: { class: 'form-horizontal' } do |f| %>
  <%= f.error_notification %>

  <%= f.input :vim_mode, as: :select, collection: @vim_modes %>
  <%= f.input :command %>
  <%= f.input :description %>

  <%= f.submit class: "btn btn-success" %>
<% end %>
